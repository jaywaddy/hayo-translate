import React from "react";

// Styles
import { 
    HeaderContainer, 
    AppTitle
} from "./HeaderStyles";

export default function Header() {
    return (
        <HeaderContainer>
            <AppTitle>HAYO</AppTitle>
        </HeaderContainer>
    );
}
